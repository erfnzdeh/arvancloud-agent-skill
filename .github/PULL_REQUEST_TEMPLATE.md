## Why

What was wrong or missing, in a sentence or two. Not a tour of the diff.

## What

## Checks

- [ ] No API key, S3 secret, or acme token in the diff
- [ ] `assets/config.example.json` still has names only
- [ ] I said which spec URL I checked, if this changes an endpoint
